#include <stdio.h>

int main(){
    input = scanf(%c);
    int sent = 0;
    int words = 0;
    int char =0;
    while (1) {
        ch = getchar();
        if (ch == "\n") {
            sent += 1;
            }
        if (ch == " ") {
            words += 1;
        }
        if (feof(stdin)) {
            break;
        if ((ch >= 'a' && ch <= 'z') || (ch >= 'A' && ch <= 'Z')){
            char += 1
        }
        printf("%d, %d, %d", sent, words, char)
        }
        
    
        

    
